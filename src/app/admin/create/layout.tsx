import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ruvz | Create",
};

export default function AdminCreateLayout({ children }: DefaultComponentProps) {
  return children;
}
